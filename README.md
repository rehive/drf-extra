# Rehive DRF Extra

## Quick start

1. Install the package:

```
pip install drf-extra
```

2. Add "drf_extra" to your INSTALLED_APPS settings like this:

```
    INSTALLED_APPS = [
        ...
        'drf_extra',
    ]
```
